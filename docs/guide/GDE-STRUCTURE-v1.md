# 汽配供应链协同系统 (Fabric Supply Chain) 项目结构分析

本项目是一个基于 Hyperledger Fabric 的典型区块链应用，专注于汽车零部件供应链的订单流转与物流追踪。采用了**分层设计**：从底层区块链网络到中间件智能合约，再到面向用户的 Web 应用。

## 🏗️ 核心架构

系统架构主要分为以下三个层面：

1.  **区块链网络层 (`network/`)**: 负责搭建分布式账本环境，采用 3 组织（Org1/2/3）模拟 4 种业务角色。
2.  **智能合约层 (`chaincode/`)**: 业务逻辑的核心，定义了订单（Order）与物流（Shipment）的生命周期。
3.  **应用层 (`application/`)**:
    -   **后端 (`server/`)**: Go 编写的 API 服务，负责处理各角色的业务请求。
    -   **前端 (`web/`)**: Vue 3 编写的用户交互界面，多角色共用一套 UI 逻辑。

---

## 📂 目录结构详解

### 1. 根目录
-   `install.sh`: 一键部署脚本，自动化启动 Fabric 网络与应用容器。
-   `uninstall.sh`: 一键清理脚本。
-   `dev.md`: 核心开发指南。
-   `README.md`: 项目简介、业务流程及快速入门。

### 2. 区块链网络层 (`network/`)
该目录包含 Fabric 网络的所有配置文件和启动脚本。

-   `configtx.yaml`: 通道配置，定义了组织机构和共识配置。
-   `crypto-config.yaml`: 证书配置，用于生成组织身份。
-   `docker-compose.yaml`: 定义了 Peer 节点、Orderer 节点及 CLI 工具。

### 3. 智能合约层 (`chaincode/`)
定义了供应链的资产模型和状态机。

-   `chaincode.go`: **核心合约逻辑**。实现了订单的创建、接受、生产状态更新、取货发货及签收确认。

### 4. 后端应用层 (`application/server/`)
基于 Gin 框架实现的 RESTful API。

-   `main.go`: 程序入口，按角色划分为 `/api/oem`, `/api/manufacturer`, `/api/carrier`, `/api/platform` 路由组。
-   `api/`: **接口处理器**。
    -   `supply_chain.go`: 供应链各角色的业务接口处理。
-   `service/`: **业务逻辑层**。
    -   `supply_chain_service.go`: 封装了与链码交互的具体逻辑。
-   `pkg/fabric/`: Fabric SDK 封装。

### 5. 前端应用层 (`application/web/`)
基于 Vue 3 + Vite + Ant Design Vue 构建。

-   `src/views/`: **页面组件**。
    -   `OrderManagement.vue`: **全角色通用的订单管理中心**。根据路由角色动态显示操作按钮。
-   `src/api/`: 基于 Axios 封装的后端接口请求。
-   `src/types/`: 定义了 `Order` 和 `Shipment` 的 TypeScript 类型。

---

## 🛠️ 二开建议

1.  **修改业务字段**:
    -   修改 `chaincode/chaincode.go` 中的 `Order` 或 `Shipment` 结构体。
    -   在 `application/server/api/supply_chain.go` 中解析新字段。
    -   在 `application/web/src/types/index.ts` 中维护类型一致性。
2.  **流程扩展**:
    -   在链码中增加新的状态（如：退货、质检）。
    -   在前端 `OrderManagement.vue` 的 `statusSteps` 中添加新步骤。
3.  **多组织适配**:
    -   本项目逻辑上拆分了 4 个角色，但物理上运行在 3 个 Org 上。若需物理隔离更多组织，需修改 `network/` 配置。

> [!TIP]
> 部署时请参考 `redeploy_guide.md` 进行镜像重建与环境清理。
